export type Service = {
  title: string;
  description: string;
  icon: string;
};

export type Testimonial = {
  id: number;
  name: string;
  role: string;
  content: string;
};

export type Stat = {
  number: string;
  label: string;
};

export type ColorPalette = {
  primary: string;
  primaryLight: string;
  secondary: string;
  accent: string;
  dark: string;
  light: string;
};